{
  "name": "@darwish-yu/pi-jev-router",
  "version": "0.2.0",
  "type": "module",
  "description": "Pi extension: route the first task of a session to a suitable model via the Jev (TypeSafe System One) decision model",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LonelyFellas/pi-jev-router.git"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.87.1",
    "@earendil-works/pi-coding-agent": "^0.87.1",
    "typescript": "^5.6.0"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
